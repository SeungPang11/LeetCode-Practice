class Solution:
    def reverseWords(self, s: str) -> str:
        s = s.split()
        res = ""
        for i in reversed(range(len(s))):
            res += s[i]
            if i != 0:
                res += " "
        
        return res
